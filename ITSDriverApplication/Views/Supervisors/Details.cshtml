@model ITSDriverApplication.Models.Supervisor

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Supervisor</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>First Name</dt>
        <dd>@Html.DisplayFor(model => model.First_Name)</dd>

        <dt>Last Name</dt>
        <dd>@Html.DisplayFor(model => model.Last_Name)</dd>

        <dt>Department</dt>
        <dd>@Html.DisplayFor(model => model.Dept)</dd>

        <dt>Email</dt>
        <dd>@Html.DisplayFor(model => model.Email)</dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Supervisor_ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>

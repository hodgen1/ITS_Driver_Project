@model IEnumerable<ITSDriverApplication.Models.Driver>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
        <th>Regis ID</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Middle Initial</th>
        <th>DOB</th>
        <th>Driver Type</th>
        <th>Driver Status</th>
        <th>Driver License Number</th>
        <th>Insurance Name</th>
        <th>Insurance Policy Number</th>
        <th>Policy Expiration Date</th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Details", "Details", new { id = item.Driver_ID })
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Regis_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.First_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Last_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Middle_Initial)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DOB)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Driver_Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Driver_Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DL_Number)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ins_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ins_Policy_Number)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ins_Policy_Exp_Date)
        </td>
        <td>
            @Html.ActionLink("Delete", "Delete", new { id=item.Driver_ID })
        </td>
    </tr>
}

</table>
